import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'no-floating-decimal', 'eslint', Options>

export default Configuration
