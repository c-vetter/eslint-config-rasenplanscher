import Configuration from './no-void.d'

const configuration:Configuration = {
	ruleId: `no-void`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
