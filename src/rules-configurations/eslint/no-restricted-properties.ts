import Configuration from './no-restricted-properties.d'

const configuration:Configuration = {
	id: 'no-restricted-properties',
	ruleId: 'no-restricted-properties',
	providerId: 'eslint',
	ignore: true,
}

export default configuration
