import Configuration from './implicit-arrow-linebreak.d'

const configuration:Configuration = {
	ruleId: `implicit-arrow-linebreak`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `beside` ],
}

export default configuration
