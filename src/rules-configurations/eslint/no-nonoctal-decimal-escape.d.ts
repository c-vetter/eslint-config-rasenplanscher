import { RuleConfiguration } from '../../../support/Rule'

type Options = never[]

type Configuration = RuleConfiguration<'no-nonoctal-decimal-escape', 'eslint', Options>

export default Configuration