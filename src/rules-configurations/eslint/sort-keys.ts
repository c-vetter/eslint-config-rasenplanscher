import Configuration from './sort-keys.d'

const configuration:Configuration = {
	id: 'sort-keys',
	ruleId: 'sort-keys',
	providerId: 'eslint',
	priority: 'TASTE',
	activate: false,
}

export default configuration
