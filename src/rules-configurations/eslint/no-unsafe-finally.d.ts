import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'no-unsafe-finally', 'eslint', Options>

export default Configuration
