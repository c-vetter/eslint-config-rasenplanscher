import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'no-duplicate-case', 'eslint', Options>

export default Configuration
