import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'block-scoped-var', 'eslint', Options>

export default Configuration
