import { RuleConfiguration } from '../../../support/Rule'

export type Options = never[]

type Configuration = RuleConfiguration<'no-template-curly-in-string', 'eslint', Options>

export default Configuration
