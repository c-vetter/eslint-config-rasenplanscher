import { RuleConfigurationOverride } from '../../../support/Rule'

import BaseConfiguration from '../eslint/comma-spacing.d'

type Configuration = RuleConfigurationOverride<BaseConfiguration, '@typescript-eslint/comma-spacing', '@typescript-eslint/eslint-plugin'>

export default Configuration