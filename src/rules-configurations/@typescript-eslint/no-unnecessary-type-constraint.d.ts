import { RuleConfiguration } from '../../../support/Rule'

type Options = never[]

type Configuration = RuleConfiguration<'@typescript-eslint/no-unnecessary-type-constraint', '@typescript-eslint/eslint-plugin', Options>

export default Configuration