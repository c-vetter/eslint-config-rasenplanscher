import { RuleConfiguration } from '../../../support/Rule'

export type Options = never[]

type Configuration = RuleConfiguration<'@typescript-eslint/no-unnecessary-type-arguments', '@typescript-eslint/eslint-plugin', Options>

export default Configuration
