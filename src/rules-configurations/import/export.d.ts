import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'import/export', 'eslint-plugin-import', Options>

export default Configuration
