import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = never[]

type Configuration = RuleConfiguration<'import/no-named-as-default-member', 'eslint-plugin-import', Options>

export default Configuration
